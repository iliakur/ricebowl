#-> ~/.{name}
[user]
	name = Ilia Kurenkov
  # Email address is deliberately left out here. It is set to different values on my personal and work machines and controlled with an envvar:
  # https://git-scm.com/book/en/v2/Git-Internals-Environment-Variables
[credential]
	helper = cache
[alias]
	ci = commit -m
[diff]

[merge]
	tool = meld
[core]
	editor = emacsclient -t --alternate-editor=\"\"
[diff]
	tool = meld
[push]
	default = simple
