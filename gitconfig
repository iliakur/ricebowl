#-> ~/.{name}
[user]
	name = Ilia Kurenkov
  # Until Magit supports Git's environment variables, there's no easy obvious way to
  # set email differently for work and home machines.
  # The simplest for now is to make sure I use `git-rtr-clone` to set those correctly per repository
  email = ilia.kurenkov@gmail.com
[credential]
	helper = cache
[alias]
	ci = commit -m
[diff]

[merge]
	tool = meld
[core]
	editor = emacsclient -t --alternate-editor=\"\"
[diff]
	tool = meld
[push]
	default = simple
